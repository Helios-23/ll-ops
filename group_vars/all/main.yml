# env
local_ip: 127.0.0.1
autobuild: true
unattended_upgrades: true
domain: 'llight.io'
fqdn: "pharos.{{ domain }}"
admin_user: "devops"
admin_email: "{{ admin_user }}@{{ domain }}"

ansible_python_interpreter: auto_silent

# keys
auth_user: viewer
auth_pass: "{{vault_all.auth_pass}}"
ansible_become_password: "{{vault_all.ansible_become_password}}"

# Ports
email_port: 587
netcat_port: 1337
postgres_port: 5432
redis_port: 6379
sonarqube_port: 9023

# ssl
ssl_dir: "/etc/ssl"
ssl_sites_dir: "{{ssl_dir}}/logicallight"

# certbot
certbot_admin_email: "{{admin_email}}"
certbot_domain_type: "single"

# gcp
gcp_owner: "rp@lligt.io"
gcp_sa: "{{ admin_email }}"
gcp_org_id: "{{vault_all.gcp_llight.org_id}}"
gcp_proj_id: "{{vault_all.gcp_llight.proj_id}}"
gcp_region: "us-west1"
gcp_zone: "us-west1-a"

# smtp email
smtp_server: "smtp-relay.brevo.com"
smtp_mail_from: "pharos_noreply@llight.io"
smtp_login: "{{ vault_all.smtp.user }}"
smtp_pass: "{{ vault_all.smtp.pass }}"


#sonarqube
monitor_ip: "{{hostvars['monitor_prd']['ansible_host']}}"
sonardb:
  name: "sonarqube"
  user: "{{vault_all.sonardb.user}}"
  pass: "{{vault_all.sonardb.pass}}"
