- name: Build Pharos release artifacts on the controller
  hosts: localhost
  connection: local
  become: false
  gather_facts: true
  vars:
    # Update repositories on the controller before building when true
    update_repo: false
  roles:
    - role: pharos_build
